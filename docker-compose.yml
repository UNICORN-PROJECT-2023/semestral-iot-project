version: "1"
services:
  db-postgres:
    image: postgres:14.5
    restart: unless-stopped
    hostname: postgres
    container_name: serverWatch-db
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment: 
      - POSTGRES_PASSWORD
      - POSTGRES_USER
    networks:
      - my-bridge-network
  frontend:
    container_name: client
    platform: linux/arm64
    image: serverWatch/frontend
    build: ./client
    ports:
      - 3000:3000
    env_file:
      - .env
    environment:
      - API_URL
    networks:
      - my-bridge-network
  backend:
    container_name: backend
    platform: linux/arm64
    image: serverWatch/backend
    build: ./server
    ports:
      - 3001:3001
    env_file:
      - .env
    environment:
      - POSTGRES_PASSWORD
      - POSTGRES_USER
      - POSTGRES_USERNAME
      - POSTGRES_HOST
      - POSTGRES_DATABASE
    networks:
      - my-bridge-network

networks:
  my-bridge-network:
    driver: bridge

volumes:
  pgdata: